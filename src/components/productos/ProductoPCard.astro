---
interface Props {
    img: string;
    title: string;
    route: string;
}

const { img, title, route } = Astro.props;

---
<div class="product-card">
    <img src={img} alt="producto" />
    <h3>{title}</h3>
    <a href={route}>
        <div class="button">
            Ver Producto
        </div>
    <a/>
</div>

<style>
    .product-card {
        background-color: white;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        border-radius: 5px;
    }
    .product-card img {
        height: 200px;
        width: 80%;
        display: flex;
        object-fit: cover;
        margin: 0 auto;
        margin-top: 2rem;
        border-radius: 5px;
    }
    .product-card h3 {
        margin: 1rem 2rem 2rem 2rem;
    }
    a {
        color: black;
        font-weight: 700;
    }
    .button {
        display: inline-block;
        margin: 0 0 2rem 2rem;
        padding: 1rem;
        background-color: var(--yellow);
        border-radius: 5px;
        box-shadow: 2px 2px 5px rgba(0,0,0,0.5);
        transition: all 0.5s;
    }
    .button:hover {
        transform: scale(1.1);
        box-shadow: 2px 2px 10px rgba(0,0,0,0.5);
    }
</style>