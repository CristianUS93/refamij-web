---
import wspLogo from "../../assets/icons-social/whatsapp.svg";

const { title, text, wspPhone } = Astro.props;
---
<div class="main-div">
    <p>{text}</p>
    <div class="buttons-div">
        <a href={`https://wa.me/${wspPhone}?text=Buenos%20días.%20Quisiera%20hacer%20algunas%20consultas%20sobre%20${title}.`} target="_blank">
            <div class="wsp button">
                <img src={wspLogo.src} alt="Whatsapp Logo">
                <span>Conversa con nuestro especialista en línea</span>
            </div>
        </a>
        <a href="">
            <div class="cot button">
                <span>Cotizar ahora</span>
            </div>
        </a>
    </div>
</div>

<style>
    .main-div {
        height: 100%;
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }
    .buttons-div {
        display: flex;
        flex-wrap: wrap;
    }

    .button {
        height: 60px;
        max-width: 250px;
        padding: 0.7rem;
        color: black;
        font-weight: 500;
        display: flex;
        align-items: center;
    }

    .wsp {
        background-color: var(--green-wsp);
        margin-right: 2rem;
    }

    .cot {
        background-color: var(--yellow);
    }

    @media screen and (max-width: 500px) {
        .buttons-div {
            flex-direction: column;
            align-items: center;
        }
        .wsp {
            margin-right: 0;
            margin-bottom: 1rem;
        }
    }
    @media screen and (max-width: 450px) {
        p {
            font-size: 13px;
        }
    }
</style>